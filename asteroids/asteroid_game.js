AsteroidGame = {};

(function (AsteroidGame) {
  var MovingObject = AsteroidGame.MovingObject = (AsteroidGame.MovingObject || {});
  var Game = AsteroidGame.Game = (AsteroidGame.Game || {});
  var Asteroid = AsteroidGame.Asteroid = (AsteroidGame.Asteroid || {});
  var Ship = AsteroidGame.Ship = (AsteroidGame.Ship || {});

})(AsteroidGame);